# files/

Static files served from `https://zixuan-chen.github.io/files/`.

Upload your CV here as **`Zixuan_Chen_CV.pdf`** — the "CV" item in
`_data/navigation.yml` already points at that exact path, so nothing else needs
to change once the file exists.

Keep this README so the directory stays in git (Git does not track empty
directories). It is excluded from the built site by `_config.yml`.
